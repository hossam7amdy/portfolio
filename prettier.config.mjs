/**
 * @see https://prettier.io/docs/en/configuration.html
 * @type {import("prettier").Config}
 */
const config = {
  trailingComma: 'es5',
  tabWidth: 2,
  useTabs: false,
  semi: true,
  printWidth: 100,
  singleQuote: true,
  arrowParens: 'avoid',
};

export default config;
